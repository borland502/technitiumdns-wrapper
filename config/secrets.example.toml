# Copy this file to ${XDG_CONFIG_HOME:-~/.config}/technitiumdns-cli/config.toml and update the credentials.
# The generated config in XDG_CONFIG_HOME overrides the embedded defaults.

# Optional API overrides; delete keys you do not need.
[api]
baseUrl = "http://localhost:5380/api"
timeoutMs = 15000
verifyTls = true

[auth]
username = "admin"
password = "change-me"
token = ""
totp = ""
